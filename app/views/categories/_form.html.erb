<div class="form-inline">
	<%= form_for @category do |f| %>
		<p>
			<%= f.label :name, "分类名称" %> 
			<%= f.text_field :name, class: "form-control" %>
		</p>
		<br /> 
		<p>
			<%= f.submit "保存", class: "btn btn-primary" %>
		</p>
	<% end %>
</div>

<%= link_to "返回列表", categories_path %>